# Supabase CLI local state
supabase/.temp/

# OS / editor
.cursor/
.DS_Store
*.log

# Dependencies (if added later)
node_modules/

# Extension build output (rebuild with: npm run ext:all)
/dist/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Staff bulk import — never commit generated secrets
scripts/*.passwords.tsv
